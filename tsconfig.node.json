{
  "compilerOptions": {
    /* 启用组合编译模式，这可以提高构建速度，特别是在大型项目中。组合编译模式允许在单个项目中管理多个 TypeScript 文件的类型信息。 */
    "composite": true,
    /* 跳过对声明文件的类型检查，可以提高构建速度。 */
    "skipLibCheck": true,
    /* 使用 ES Modules 进行模块化，通过导入和导出语法来管理代码模块。 */
    "module": "ESNext",
    /* 使用捆绑器模块解析策略，将模块解析交给捆绑器（如 Webpack）处理。 */
    "moduleResolution": "bundler",
    /* 允许从没有默认导出的模块中导入默认导出。这在一些情况下可能会很有用，特别是在使用 CommonJS 模块的情况下。 */
    "allowSyntheticDefaultImports": true
  },
  /* 指定要包含的源代码文件，这里只包括了一个  */
  "include": ["vite.config.ts"]
}
